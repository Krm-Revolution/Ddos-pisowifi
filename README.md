```text
git clone https://github.com/Krm-Revolution/Ddos-pisowifi.git
```
### USAGE
---
### python Ddos.py -s <ip> -m <mode> [-p <port>] [-t <threads>]
    -h : help
    -s : target ip
    -m : mode (tcp / udp / both)
    -p : port (default 80)
    -t : threads (default 500)
---
